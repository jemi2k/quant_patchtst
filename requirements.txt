# Quant PatchTST — project dependencies

# --- Core deep learning ---
torch>=2.1              # Dual-Head PatchTST + PCGrad (Linux pip wheel bundles CUDA)

# --- Data & numerics ---
numpy>=1.24             # array backend (attention mask already imports it)
pandas>=2.0             # DIB OHLC / aggTrades ingestion & reshaping (Phase 2)

# --- Data engineering (Phase 2) ---
aiohttp>=3.9            # async HTTP client for bulk aggTrades download
pyarrow>=16.0           # streaming parquet I/O for tick data

# --- Statistics ---
statsmodels>=0.14       # Augmented Dickey-Fuller for Fractional Diff (Phase 2)

# --- Modeling utilities ---
scikit-learn>=1.3       # CPCV splits, metrics, general utilities (Phase 5)
matplotlib>=3.7         # attention-weight plots (self.attns) + backtest charts
